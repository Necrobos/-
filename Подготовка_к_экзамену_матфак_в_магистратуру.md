# Подготовка к вступительному экзамену в магистратуру — 2025

Конспект собран по четырём материалам:
- «Функции многих переменных» (учебно-методическое пособие ВГУ, 2011)
- «Справочный материал по дифференциальным уравнениям» (теорема Коши–Пикара, устойчивость по Ляпунову)
- «Список вопросов» (программа экзамена по трём разделам)
- Образец КИМ №7

Билет состоит из **трёх разделов**: I. Алгебра, II. Математический анализ, III. Дифференциальные уравнения. Теорию нужно знать не «по учебнику», а в объёме кратких формулировок; акцент — на умении решать типовые задачи.

**Система оценивания (по образцу КИМ):**

| Раздел | Задание | Макс. балл |
|---|---|---|
| I. Алгебра | 1 (теория) | 5 |
| | 2 (теория) | 5 |
| | 3 (тест-обоснование) | 10 |
| | 4 (вычисление) | 10 |
| II. Матанализ | 1 (теория) | 5 |
| | 2 (теория) | 10 |
| | 3 (тест-обоснование) | 15 |
| III. ДУ | 1 (теория) | 10 |
| | 2 (задача) | 15 |
| | 3 (задача) | 15 |

---

## Раздел I. Алгебра

### Что нужно знать из теории
- **Линейные пространства.** Примеры (R^n, C[a,b], пространства многочленов и т.д.). Базис и размерность линейного пространства.
- **Линейные операторы.** Примеры. Матрица линейного оператора в заданном базисе. Связь матриц одного и того же оператора в разных базисах.
- **Системы линейных уравнений.** Метод Гаусса.

> В этом разделе не будет громоздких вычислений: максимум — решение системы 3×3 методом Гаусса или вычисление определителя матрицы 3×3. Первые два вопроса билета — теоретические (определение или формулировка теоремы без доказательства), третье и четвёртое задания — тест с обоснованием либо вычисление.

### Ключевые факты

**Базис и размерность.** Базис линейного пространства — это упорядоченный набор линейно независимых векторов, через который линейно выражается любой вектор пространства. Все базисы конечномерного пространства состоят из одного и того же числа векторов — это число называется размерностью пространства. (Внимание: утверждение «базис всегда конечен» и «базис единственен» — **ложны** в общем случае; единственность базиса верна только для нулевого пространства.)

**Линейный оператор** — отображение $A:V\to V$ линейного пространства в себя (или в другое линейное пространство), сохраняющее операции сложения и умножения на скаляр:
$$A(x+y)=Ax+Ay,\qquad A(\lambda x)=\lambda Ax.$$

**Матрица линейного оператора** — матрица, столбцы которой — координаты образов базисных векторов в этом же базисе.

**Связь матриц оператора в разных базисах.** Если $T$ — матрица перехода от базиса $e$ к базису $f$, а $A_e$, $A_f$ — матрицы оператора в этих базисах, то
$$A_f = T^{-1} A_e\, T.$$

**Ранг матрицы** и системы линейных уравнений: система $Ax=b$ совместна тогда и только тогда, когда ранг матрицы системы равен рангу расширенной матрицы (теорема Кронекера–Капелли); число свободных переменных однородной системы равно $n-\operatorname{rk}A$.

### Разбор примеров из образца КИМ

**Задача.** Вычислить собственные значения оператора, заданного матрицей
$$
A=\begin{pmatrix}1 & 2 & 0\\ 0 & 2 & 0\\ 3 & 0 & -1\end{pmatrix}.
$$

*Решение.* Матрица блочно-треугольная (третий столбец — только на диагонали), поэтому характеристический многочлен раскладывается по первому столбцу/структуре: собственные значения — корни $\det(A-\lambda E)=0$. Ответ из образца: $\lambda_1=-1,\ \lambda_2=1,\ \lambda_3=2$.

**Пример теста-рассуждения (с обоснованием, по духу задания 1 из списка вопросов):**
Пусть $V$ — линейное пространство над $\mathbb R$. Из утверждений о базисе верны:
- размерность пространства равна количеству векторов в любом его базисе — **верно** (это и есть определение размерности);
- размерность пространства может быть равна нулю только для тривиального (нулевого) пространства — **верно**.

Остальные варианты (базис всегда конечен, базис единственен, базис состоит из ортогональных векторов) — **неверны**: контрпримеры — пространства бесконечной размерности (например, $C[a,b]$), существование разных базисов у одного пространства, неортогональные базисы (например, $\{(1,0),(1,1)\}$ в $\mathbb R^2$).

### Практика к разделу I
- Уверенно решать СЛУ методом Гаусса (в т.ч. с параметром).
- Вычислять определитель матрицы 3×3 (разложением по строке/столбцу или треугольным приведением).
- Находить собственные значения матрицы 3×3 через характеристическое уравнение $\det(A-\lambda E)=0$.
- Уметь находить обратную матрицу любым способом (через присоединённую (союзную) матрицу или методом Гаусса $[A|E]\to[E|A^{-1}]$).
- Помнить формулу смены базиса $A_f=T_{ef}^{-1}A_eT_{ef}$ и уметь узнавать её среди похожих неверных вариантов.

---

## Раздел II. Математический анализ

### 2.1. Числовые последовательности

**Что нужно знать.** Понятие числовой последовательности, сходящиеся и расходящиеся последовательности, критерий Коши сходимости последовательности (последовательность $\{a_n\}$ сходится $\iff$ она фундаментальна: $\forall\varepsilon>0\ \exists N\ \forall n,m>N:\ |a_n-a_m|<\varepsilon$).

**Типовые пределы** (образец из списка вопросов):
$$
\lim_{n\to\infty}\frac{n}{1+n^2}=0,\qquad
\lim_{n\to\infty}\frac{n^3+2}{1+n^2}=+\infty,\qquad
\lim_{n\to\infty}\frac{n^3+4}{1+4n^3}=\frac14.
$$
Приём во всех трёх случаях один: сравнить старшие степени числителя и знаменателя (разделить на них).

---

### 2.2. Функции многих переменных

Это самый объёмный теоретический блок — целиком разобран в пособии «Функции многих переменных» (ВГУ, 2011). Ниже — теория и **все примеры пособия с решениями**.

#### 2.2.1. Понятие функции $n$ переменных

Пусть $E\subset\mathbb R^n$. Если каждой точке $M(x_1,\dots,x_n)\in E$ поставлено в соответствие число $u$, то на $E$ определена **функция $n$ переменных** $u=f(M)=f(x_1,\dots,x_n)$. $E$ — область определения, $M$ — аргумент. Для двух и трёх переменных пишут $u=f(x,y)$, $u=f(x,y,z)$.

#### 2.2.2. Предел функции

**Определение по Коши.** Число $b$ называется пределом функции $f(M)$ в точке $A$ при $M\to A$ (записывается $\lim_{M\to A}f(M)=b$), если
$$
\forall\varepsilon>0\ \exists\delta>0\ \forall M:\ (M\in E,\ 0<\rho(M,A)<\delta)\Rightarrow |f(M)-b|<\varepsilon,
$$
где $\rho(M,A)$ — одна из эквивалентных метрик:
$$
\rho(M,A)=\sqrt{\textstyle\sum(x_k-a_k)^2},\quad \max_k|x_k-a_k|,\quad \sum_k|x_k-a_k|.
$$

**Определение по Гейне.** Число $b$ — предел $f(M)$ в точке $A$, если для *любой* последовательности $M_k\to A$ ($M_k\in E$, $M_k\ne A$) соответствующая последовательность $f(M_k)\to b$. Определения по Коши и по Гейне эквивалентны.

Для функции двух переменных предел $\lim_{x\to x_0,\,y\to y_0}f(x,y)$ называют **двойным пределом**.

**Предел на бесконечности.** $\lim_{M\to\infty}f(M)=b$, если $\forall\varepsilon>0\ \exists R>0\ \forall M:(M\in E,\ \rho(M,O)>R)\Rightarrow|f(M)-b|<\varepsilon$.

**Предел по кривой.** Пусть $\Gamma\subset E$ — непрерывная кривая через $A$. Тогда $\lim_{M\to A,\,M\in\Gamma}f(M)=b$, если $\forall\varepsilon>0\ \exists\delta>0\ \forall M\in\Gamma:\ 0<\rho(M,A)<\delta\Rightarrow|f(M)-b|<\varepsilon$. Для $y=y(x)$ через $(x_0,y_0)$:
$$
\lim_{\substack{x\to x_0\\ y=y(x)}}f(x,y)=\lim_{x\to x_0}f(x,y(x)).
$$

**Как доказать, что предела не существует** (два эквивалентных способа):
1. Указать **две последовательности** $M_n^1,M_n^2\to A$, для которых $\lim f(M_n^1)\ne\lim f(M_n^2)$;
2. Указать **две различные кривые** $y=y_1(x)$, $y=y_2(x)$ через $(x_0,y_0)$, вдоль которых пределы функции различны.

**Пример 1.** Доказать, что $\displaystyle\lim_{x\to0,\,y\to0}\frac{x^2y}{x^2+y^2}=0$.

*Решение.* Так как $y^2\ge0$, то $x^2\le x^2+y^2$, значит $\dfrac{x^2}{x^2+y^2}\le1$. Тогда
$$
\left|\frac{x^2y}{x^2+y^2}-0\right|=\frac{x^2}{x^2+y^2}\cdot|y|\le|y|=\sqrt{y^2}\le\sqrt{x^2+y^2}.
$$
Значит, для любого $\varepsilon>0$ достаточно взять $\delta=\varepsilon$: при $\sqrt{x^2+y^2}<\delta$ имеем $\left|\frac{x^2y}{x^2+y^2}\right|<\varepsilon$. По определению 1 предел равен $0$. $\blacksquare$

**Пример 2.** Доказать, что $\displaystyle\lim_{x\to0,\,y\to0}\frac{2xy}{x^2+y^2}$ **не существует**.

*Способ 1 (по Гейне).* Возьмём $(x_n^1,y_n^1)=(1/n,1/n)\to(0,0)$ и $(x_n^2,y_n^2)=(1/n^2,1/n)\to(0,0)$. Тогда
$$
f(x_n^1,y_n^1)=\frac{2\cdot\frac1n\cdot\frac1n}{\frac1{n^2}+\frac1{n^2}}=1\to1,\qquad
f(x_n^2,y_n^2)=\frac{2\cdot\frac1{n^2}\cdot\frac1n}{\frac1{n^4}+\frac1{n^2}}=\frac{2n}{n^2+1}\to0.
$$
Пределы различны — предел функции не существует.

*Способ 2 (по прямым $y=kx$).* $\displaystyle\lim_{x\to0}f(x,kx)=\lim_{x\to0}\frac{2kx^2}{(1+k^2)x^2}=\frac{2k}{1+k^2}$ — зависит от $k$, значит предел не существует. $\blacksquare$

**Пример 3.** Найти предел $f(x,y)=x^2e^{-(x^2-y)}$ вдоль луча $x=t\cos\alpha,\ y=t\sin\alpha$ при $t\to+\infty$. Является ли функция бесконечно малой при $x\to+\infty,\ y\to+\infty$?

*Решение.* Вдоль луча:
$$
\lim_{t\to+\infty}t^2\cos^2\alpha\cdot e^{-(t^2\cos^2\alpha-t\sin\alpha)}=\cos^2\alpha\lim_{t\to+\infty}t^2e^{-(t^2\cos^2\alpha-t\sin\alpha)}=0
$$
(по правилу Лопиталя — для любого фиксированного $\alpha$). Но вдоль параболы $x=t,\ y=t^2$:
$$
\lim_{t\to+\infty}f(t,t^2)=\lim_{t\to+\infty}t^2e^{(-t^2+t^2)}=\lim_{t\to+\infty}t^2=+\infty.
$$
Значит, вдоль разных кривых пределы разные — двойной предел **не существует**, функция не является б.м. при $x,y\to+\infty$. $\blacksquare$

> Важный вывод из примеров 2–3: совпадение пределов вдоль *всех прямых* (или всех лучей) ещё не гарантирует существования двойного предела — нужно проверять произвольные кривые (степенные, параболы и т.п.).

#### 2.2.3. Повторные пределы

Для $u=f(x,y)$, определённой в проколотой окрестности $(x_0,y_0)$: если при каждом фиксированном $y$ существует $\lim_{x\to x_0}f(x,y)=\varphi(y)$, а затем существует $\lim_{y\to y_0}\varphi(y)=b$, то говорят, что существует **повторный предел** $\displaystyle\lim_{y\to y_0}\lim_{x\to x_0}f(x,y)=b$. Аналогично определяется второй повторный предел $\lim_{x\to x_0}\lim_{y\to y_0}f(x,y)$.

**Теорема 1.** Если существует двойной предел $\lim_{x\to x_0,y\to y_0}f(x,y)=b$ и существуют оба внутренних предела, то оба повторных предела тоже равны $b$.

**Обратное неверно** — три классических контрпримера:

**Пример 4.** $f(x,y)=\dfrac{x-y}{x+y}$: оба повторных предела существуют, но не равны ($1$ и $-1$), а двойной предел не существует.
$$
\lim_{x\to0}\Big\{\lim_{y\to0}f\Big\}=\lim_{x\to0}\frac{x}{x}=1,\qquad
\lim_{y\to0}\Big\{\lim_{x\to0}f\Big\}=\lim_{y\to0}\frac{-y}{y}=-1.
$$
Двойного предела нет: по $y=kx$, $\lim_{x\to0}f(x,kx)=\frac{1-k}{1+k}$ зависит от $k$.

**Пример 5.** $f(x,y)=\dfrac{x^2y^2}{x^2y^2+(x-y)^2}$: оба повторных предела равны $0$ и совпадают, но двойного предела всё равно нет — вдоль $y=x$ предел равен $1$, вдоль $y=-x$ предел равен $0$. Значит, **равенство повторных пределов не гарантирует существования двойного предела**.

**Пример 6.** $f(x,y)=(x+y)\sin\frac1x\sin\frac1y$: **оба** повторных предела не существуют (так как $\lim_{y\to0}\sin\frac1y$ не существует), но двойной предел существует и равен $0$, поскольку
$$
\left|(x+y)\sin\frac1x\sin\frac1y\right|\le|x|+|y|\xrightarrow[x,y\to0]{}0.
$$
Значит, **существование двойного предела не требует существования повторных**.

#### 2.2.4. Непрерывность функции $n$ переменных

$f(M)$ непрерывна в предельной точке $A\in D$, если $\lim_{M\to A}f(M)=f(A)$. Точки, где это нарушено, — точки разрыва.

**Непрерывность по переменной** (частная непрерывность): $f(x,y)$ непрерывна в $(x_0,y_0)$ по $x$, если $\lim_{x\to x_0}f(x,y_0)=f(x_0,y_0)$ (аналогично по $y$). Обычная непрерывность (по совокупности переменных) $\Rightarrow$ непрерывность по каждой переменной, но **не наоборот**.

**Пример 7.** Найти точки разрыва $u=\dfrac{x+y}{x^3+y^3}$.

*Решение.* Функция не определена на прямой $y=-x$ (там $x^3+y^3=0$). В остальных точках она непрерывна как суперпозиция непрерывных функций. При $a\ne0$:
$$
\lim_{\substack{x\to a\\ y\to-a}}\frac{x+y}{x^3+y^3}=\lim_{\substack{x\to a\\y\to-a}}\frac1{x^2-xy+y^2}=\frac1{3a^2},
$$
— конечен, значит $A(a,-a),\ a\ne0$ — точки **устранимого разрыва**. В $O(0,0)$: $\lim\frac1{x^2-xy+y^2}=\infty$ — точка **неустранимого (бесконечного) разрыва**. $\blacksquare$

**Пример 8.** $f(x,y)=\dfrac{2xy}{x^2+y^2}$ при $x^2+y^2\ne0$, $f(0,0)=0$. Показать: непрерывна по каждой переменной в $O$, но не непрерывна по совокупности.

*Решение.* $f(x,0)=0=f(0,0)$ и $f(0,y)=0=f(0,0)$ — непрерывность по $x$ и по $y$ есть. Но вдоль $y=kx$: $\lim_{x\to0}f(x,kx)=\frac{2k}{k^2+1}$ зависит от $k$ — двойного предела нет, значит нет непрерывности по совокупности переменных. $\blacksquare$ (Это и есть иллюстрация «теорема 2 не обратима».)

**Пример 9.** $f(x,y)=\dfrac{x^2y}{x^4+y^2}$ ($f(0,0)=0$): непрерывна вдоль **каждого луча** через $O$, но не непрерывна в $O$ по совокупности.

*Решение.* Вдоль луча $x=t\cos\alpha,\ y=t\sin\alpha$:
$$
\lim_{t\to0}\frac{t^3\cos\alpha\sin\alpha}{t^2(t^2\cos^4\alpha+\sin^2\alpha)}=\lim_{t\to0}\frac{t\cos\alpha\sin\alpha}{t^2\cos^4\alpha+\sin^2\alpha}=\frac{0}{\sin^2\alpha}=0=f(0,0)\quad(\alpha\ne0,\pi).
$$
Но вдоль параболы $y=x^2$: $\lim_{x\to0}\dfrac{x^4}{x^4+x^4}=\dfrac12\ne0$. Значит непрерывности по совокупности нет — **лучевая непрерывность слабее непрерывности по совокупности**. $\blacksquare$

#### 2.2.5. Частные производные и дифференцируемость

**Частная производная:**
$$
\frac{\partial f}{\partial x}(x_0,y_0)=\lim_{\Delta x\to0}\frac{f(x_0+\Delta x,y_0)-f(x_0,y_0)}{\Delta x},
$$
аналогично по $y$ (и по любой $x_k$ для функции $m$ переменных — остальные переменные фиксируются).

**Дифференцируемость (определение 7).** $u=f(x_1,\dots,x_m)$ дифференцируема в $M$, если её полное приращение представимо в виде
$$
\Delta u = A_1\Delta x_1+\dots+A_m\Delta x_m+\alpha(\rho),\qquad \rho=\sqrt{\Delta x_1^2+\dots+\Delta x_m^2},\ \ \alpha(\rho)=o(\rho).
$$
Линейная часть $A_1\Delta x_1+\dots+A_m\Delta x_m$ — **дифференциал** $df$.

**Теорема 3 (необходимое условие).** Если $f$ дифференцируема в $M$, то в $M$ существуют все частные производные и $A_k=\partial u/\partial x_k(M)$, т.е.
$$
df=\frac{\partial f}{\partial x_1}dx_1+\dots+\frac{\partial f}{\partial x_n}dx_n.
$$
**Обратное неверно** — существование частных производных не влечёт дифференцируемости (примеры 10, 11).

**Теорема 4 (достаточное условие).** Если частные производные существуют в окрестности $M$ и **непрерывны** в $M$, то $f$ дифференцируема в $M$.

**Теорема 5.** Дифференцируемость в точке $\Rightarrow$ непрерывность в этой точке.

Для двух переменных, при $(x,y)\to(x_0,y_0)$:
$$
\Delta f(x_0,y_0)=f(x,y)-f(x_0,y_0)=f_x'(x_0,y_0)(x-x_0)+f_y'(x_0,y_0)(y-y_0)+o\!\left(\sqrt{(x-x_0)^2+(y-y_0)^2}\right).
$$

**Пример 10.** $f(x,y)=\sqrt[3]{xy}$. Найти $f_x'(0,0),f_y'(0,0)$ и проверить дифференцируемость.

*Решение.* $f(x,0)=f(0,y)=0$, поэтому $f_x'(0,0)=f_y'(0,0)=0$. Если бы $f$ была дифференцируема, то $\sqrt[3]{xy}=o(\sqrt{x^2+y^2})$, т.е. $\lim_{x,y\to0}\frac{\sqrt[3]{xy}}{\sqrt{x^2+y^2}}=0$. Но при $x=1/n,\ y=1/n^2$:
$$
\frac{\sqrt[3]{xy}}{\sqrt{x^2+y^2}}=\frac{\sqrt[3]{1/n^3}}{\sqrt{1/n^2+1/n^4}}=\frac1{\sqrt{1+1/n^2}}\to1\ne0.
$$
Противоречие — **не дифференцируема**, хотя обе частные производные существуют. $\blacksquare$

**Пример 11.** $f(x,y)=\sqrt[3]{x^3+y^3}$. Аналогично $f_x'(0,0)=f_y'(0,0)=1$ (т.к. $f(x,0)=x$, $f(0,y)=y$). Условие дифференцируемости дало бы $\lim_{x,y\to0}\frac{\sqrt[3]{x^3+y^3}-x-y}{\sqrt{x^2+y^2}}=0$, но вдоль $y=kx$ это выражение стремится к $\frac{\sqrt[3]{1+k^3}-(1+k)}{\sqrt{1+k^2}}$, которое зависит от $k$ (не равно тождественно нулю) — предел не существует, значит **не дифференцируема**. $\blacksquare$

**Пример 12.** $f(x,y)=e^{-1/(x^2+y^2)}$, $f(0,0)=0$. Проверить дифференцируемость.

*Решение.* $f_x'(0,0)=\lim_{\Delta x\to0}\frac{e^{-1/\Delta x^2}}{\Delta x}=\lim_{t\to+\infty}te^{-t^2}=0$ (замена $t=1/\Delta x$), аналогично $f_y'(0,0)=0$. Условие дифференцируемости: $e^{-1/(x^2+y^2)}=o(\sqrt{x^2+y^2})$, т.е. $e^{-t^2}=o(1/t)$ при $t\to+\infty$ — верно (экспонента убывает быстрее любой степени). Значит **дифференцируема**. $\blacksquare$

**Смешанные частные производные второго порядка.**

**Теорема 6.** Если $f_{xy}''$ и $f_{yx}''$ существуют в окрестности $(x_0,y_0)$ и **непрерывны** в этой точке, то $f_{xy}''(x_0,y_0)=f_{yx}''(x_0,y_0)$ (равенство не зависит от порядка дифференцирования).

**Пример 13.** $f(x,y)=xy\dfrac{x^2-y^2}{x^2+y^2}$ ($f(0,0)=0$). Показать $f_{xy}''(0,0)\ne f_{yx}''(0,0)$.

*Решение (кратко).* Вычисляя $f_x'(x,y)$ вне нуля и используя $f_x'(0,y)=-y$, получаем $f_{xy}''(0,0)=\lim_{\Delta y\to0}\frac{-\Delta y-0}{\Delta y}=-1$. Аналогично из $f_y'(x,0)=x$ получаем $f_{yx}''(0,0)=1$. Значит $-1\ne1$ — смешанные производные различны (нарушено условие непрерывности из теоремы 6). $\blacksquare$

**Пример 14.** $f(x,y)=\dfrac{2xy}{x^2+y^2}$ ($f(0,0)=0$): существует ли $f_{xy}''(0,0)$?

*Решение.* $f_x'(0,0)=0$; при $y\ne0$: $f_x'(0,y)=2/y$. Тогда
$$
f_{xy}''(0,0)=\lim_{\Delta y\to0}\frac{2/\Delta y-0}{\Delta y}=\lim_{\Delta y\to0}\frac{2}{\Delta y^2}=+\infty
$$
— **не существует** (предел бесконечен). $\blacksquare$

#### Возможная формулировка задания (из списка вопросов)
*Верно ли, что $u=f(x,y,z)=\sqrt[3]{x^2+y^2+z^2}$ дифференцируема в точке $(0,0,0)$?*

По аналогии с примером 10 (степенной корень из суммы квадратов/произведения): проверяем частные производные в нуле и предел $\dfrac{f(x,y,z)}{\sqrt{x^2+y^2+z^2}}$. Так как $f=(x^2+y^2+z^2)^{1/3}=\rho^{2/3}$, где $\rho=\sqrt{x^2+y^2+z^2}$, то $f_x'(0,0,0)=\lim_{\Delta x\to0}\frac{|\Delta x|^{2/3}}{\Delta x}$ — этот предел **не существует** (разные знаки при $\Delta x\to0^+$ и $\Delta x\to0^-$ дают разные бесконечности), значит частная производная в нуле не существует, и функция **не дифференцируема** в $(0,0,0)$ (не выполнено даже необходимое условие теоремы 3).

---

### 2.3. Числовые ряды

**Что нужно знать.** Сходимость числового ряда, абсолютная и условная сходимость. Признаки: Даламбера, сравнения, радикальный Коши, Лейбница.

- **Признак Даламбера.** Если $a_n\ge0$ и $q=\lim_{n\to\infty}\frac{a_{n+1}}{a_n}<1$, ряд $\sum a_n$ сходится; если $q>1$ — расходится.
- **Признак сравнения.** Если $0\le a_n\le b_n$ и $\sum b_n$ сходится, то и $\sum a_n$ сходится (и наоборот для расходимости).
- **Радикальный признак Коши.** $q=\lim\sqrt[n]{a_n}<1\Rightarrow$ сходится.
- **Признак Лейбница** (для знакочередующихся рядов $\sum(-1)^na_n$, $a_n\downarrow0$): ряд сходится.

**Разбор заданий (по типу задания 3 из списка вопросов):**

а) Ряд $\sum a_n$, $a_n\ge0$, сходится, если $q=\lim\frac{a_{n+1}}{a_n}<1$ — **верно** (признак Даламбера).

б) Если $\lim a_n=0$, то ряд сходится — **неверно**; контрпример: гармонический ряд $\sum\frac1n$, $\frac1n\to0$, но ряд расходится.

в) $\sum \dfrac{10^n}{n!}$ сходится — **верно**: $\dfrac{a_{n+1}}{a_n}=\dfrac{10}{n+1}\to0<1$ (Даламбер).

г) $\displaystyle\sum(-1)^n\frac{\operatorname{arctg}n^2}{2+n^3}$ сходится **абсолютно** — **верно**: $\left|(-1)^n\frac{\operatorname{arctg}n^2}{2+n^3}\right|=\frac{\operatorname{arctg}n^2}{2+n^3}\le\frac{\pi/2}{2+n^3}$, а ряд $\sum\frac1{2+n^3}$ сходится (эквивалентен $\sum\frac1{n^3}$, признак сравнения) — значит исходный ряд мажорируется сходящимся, абсолютная сходимость доказана.

д) Тот же ряд сходится **условно** — **неверно**, так как он сходится абсолютно (см. пункт г — абсолютная сходимость сильнее условной).

е) $\displaystyle\sum(-1)^n\frac3{2+n^3}$ — по невнимательности можно принять за аналог г); нужно смотреть на конкретный ряд из задания: если ряд из модулей расходится, а сам ряд сходится по Лейбницу — это условная сходимость. *(Общая идея примера из методички: ряд «выглядит похоже» на предыдущий, но при изменении степени знаменателя ряд из модулей начинает расходиться — тогда сходимость только условная по признаку Лейбница.)*

**Общий алгоритм для тестов на ряды:** сначала проверяем абсолютную сходимость (через мажоранту/признак Даламбера/Коши), и только если ряд из модулей расходится — проверяем условную сходимость через признак Лейбница (знакочередование + монотонное убывание общего члена к нулю).

---

### 2.4. Функциональные ряды

**Что нужно знать.** Сходимость функционального ряда в точке; равномерная сходимость на множестве; достаточные условия равномерной сходимости (признак Вейерштрасса); теорема о непрерывности суммы функционального ряда.

**Признак Вейерштрасса.** Если $|u_n(x)|\le c_n$ для всех $x\in E$, всех $n$, и числовой ряд $\sum c_n$ сходится, то функциональный ряд $\sum u_n(x)$ сходится на $E$ **равномерно** (и, следовательно, абсолютно в каждой точке).

**Теорема о непрерывности суммы.** Если все $u_n(x)$ непрерывны на $E$ и ряд $\sum u_n(x)$ сходится на $E$ равномерно, то сумма ряда непрерывна на $E$.

**Разбор заданий из образца КИМ (раздел II «Функциональные ряды»):**

а) Область сходимости степенного ряда вида $\displaystyle\sum_{n=1}^\infty \frac{n}{4^{3n}}x^n$ — типовая задача на радиус сходимости: находим $R=\lim\left|\dfrac{a_n}{a_{n+1}}\right|$ и отдельно проверяем поведение на концах интервала $(-R,R)$.

б) Аналогично для ряда вида $\sum \dfrac{x^n}{n^2\cdot 2^n}$: радиус сходимости $R=2$, на концах $x=\pm2$ ряд $\sum\frac{(\pm1)^n}{n^2}$ сходится абсолютно (по интегральному признаку/признаку сравнения с $\sum1/n^2$), значит область сходимости — **замкнутый** отрезок $[-2,2]$ (не открытый интервал, как могло бы показаться).

в) Если $|u_n(x)|\le \dfrac1{n^2}$ для всех $x\in E$ и всех $n$, то по признаку Вейерштрасса $\sum1/n^2$ сходится, значит ряд равномерно сходится на $E$ — **верно**.

г) Если $|u_n(x)|\le \dfrac1n$ для всех $x\in E$, всех $n$ — **неверно**: мажоранта $\sum1/n$ (гармонический ряд) расходится, поэтому признак Вейерштрасса неприменим — равномерная сходимость **не гарантирована** этой оценкой (нужен другой признак или пример прямо опровергает утверждение).

д)/е) Ряд вида $\displaystyle\sum_{n=1}^\infty \frac{\sin(2nx)}{\sqrt n(n+1)}x^n$ на $[0,1]$: если ряд сходится **равномерно**, то по теореме о непрерывности суммы функционального ряда сумма непрерывна. Утверждение «ряд сходится, но сумма может быть разрывной» верно только в отсутствие равномерной сходимости; если равномерная сходимость установлена (например, через мажоранту, не зависящую от $x$), то сумма обязана быть непрерывной, и правильный ответ — вариант «сходится к непрерывной функции».

> **Практический совет:** в тестах на функциональные ряды сначала определяйте, оценка какого типа дана — зависит ли мажоранта $c_n$ от $x$. Если не зависит и $\sum c_n<\infty$ — Вейерштрасс гарантирует равномерную сходимость и, значит, непрерывность суммы.

---

## Раздел III. Дифференциальные уравнения

### 3.1. Простейшие ДУ и задача Коши

Знать: уравнения с разделяющимися переменными, линейные однородные уравнения; понятие задачи Коши — уравнение $x'=f(t,x)$ с начальным условием $x(t_0)=x_0$.

### 3.2. Теорема Коши–Пикара (существование и единственность решения задачи Коши)

Рассмотрим задачу Коши
$$
x' = f(t,x),\quad t\in I,\qquad x(t_0)=x_0,\ \ t_0\in I.
$$
**Условия теоремы:** $f:I\times\mathbb R\to\mathbb R$, где
1. $f(t,x)$ непрерывна по $t$ при каждом фиксированном $x$;
2. $f(t,x)$ удовлетворяет по $x$ **условию Липшица** с константой $L$:
$$
\exists L\ge0\ \ \forall t\in I\ \ \forall x,y\in\mathbb R:\ \ |f(t,x)-f(t,y)|\le L|x-y|.
$$

**Заключение.** При выполнении этих условий задача Коши для любого $x_0$ имеет **единственное** решение на всём промежутке $I$.

**Пример 1.** Проверить условия теоремы Коши–Пикара и найти решение задачи $\dfrac{dx}{dt}=x\sin\!\left(t+\dfrac\pi2\right),\ x(0)=1$.

*Решение.* Разделяем переменные:
$$
\frac{dx}{x}=\sin\!\Big(t+\frac\pi2\Big)dt\ \Longrightarrow\ \ln|x|=-\cos\!\Big(t+\frac\pi2\Big)+c\ \Longrightarrow\ x(t)=ce^{-\cos(t+\pi/2)}.
$$
Из начального условия: $x(0)=ce^{-\cos(\pi/2)}=ce^0=c=1$, откуда
$$
x(t)=e^{-\cos(t+\pi/2)}.
$$
При делении на $x$ теряется решение $x\equiv0$ — оно решение ДУ, но не удовлетворяет начальному условию $x(0)=1$, поэтому отбрасывается. Получено единственное решение — что согласуется с теоремой (правая часть $f(t,x)=x\sin(t+\pi/2)$ липшицева по $x$ с $L=1$). $\blacksquare$

**Пример 2 (условие теоремы нарушено).** $\dfrac{dx}{dt}=x^{1/4},\ x(0)=0$.

*Решение.* Здесь $f(t,x)=x^{1/4}$ **не** удовлетворяет условию Липшица по $x$ в окрестности $x=0$ (производная $\frac14x^{-3/4}\to\infty$ при $x\to0$). Разделяя переменные, находим два разных решения, оба удовлетворяющие начальному условию:
$$
x\equiv0 \qquad\text{и}\qquad x(t)=\Big(\tfrac34 t\Big)^{4/3}.
$$
Единственности нет — это и демонстрирует, зачем нужно условие Липшица. $\blacksquare$

### 3.3. Устойчивость по Ляпунову (по первому приближению)

**Алгоритм.** Для нелинейной системы
$$
\dot x=f_1(x,y),\qquad \dot y=f_2(x,y)
$$
с нулевым положением равновесия $f_1(0,0)=f_2(0,0)=0$:

1. Составляем матрицу Якоби
$$
A(x,y)=\begin{pmatrix}
\dfrac{\partial f_1}{\partial x} & \dfrac{\partial f_1}{\partial y}\\[2mm]
\dfrac{\partial f_2}{\partial x} & \dfrac{\partial f_2}{\partial y}
\end{pmatrix}.
$$
2. Находим $A(0,0)$ и её собственные значения $\lambda_{1,2}$.
3. **Вывод (теорема Ляпунова об устойчивости по первому приближению):**
   - если $\operatorname{Re}\lambda_i<0$ для всех $i$ — нулевое решение **экспоненциально устойчиво**;
   - если хотя бы у одного $\lambda_i$ $\operatorname{Re}\lambda_i>0$ — нулевое решение **неустойчиво**;
   - если $\operatorname{Re}\lambda_i\le0$ и есть $\lambda_i$ с $\operatorname{Re}\lambda_i=0$ (без положительных) — теорема **неприменима** (нужен более тонкий анализ, метод функций Ляпунова и т.п.).

**Полезный факт (без нахождения корней явно):** для квадратного уравнения $\lambda^2+p\lambda+q=0$ оба корня имеют отрицательную вещественную часть тогда и только тогда, когда $p>0$ и $q>0$ (критерий Рауса–Гурвица для 2×2). Это часто позволяет сделать вывод об устойчивости, не решая уравнение явно.

**Пример.** Исследовать на устойчивость нулевое решение системы
$$
\dot x=\ln(4y+e^{-3x}),\qquad \dot y=\sqrt{2y-1}+\sqrt{1-4x}.
$$

*Решение.* Здесь $f_1(x,y)=\ln(4y+e^{-3x})$, $f_2(x,y)=\sqrt{2y-1}+\sqrt{1-4x}$. Вычисляя частные производные и подставляя $(0,0)$, получаем
$$
A(0,0)=\begin{pmatrix}-3 & 4\\ -2 & -2\end{pmatrix}.
$$
Характеристическое уравнение:
$$
\begin{vmatrix}-3-\lambda & 4\\ -2 & -2-\lambda\end{vmatrix}=0
\ \Longrightarrow\ \lambda^2+\lambda+2=0
\ \Longrightarrow\ \Big(\lambda+\tfrac12\Big)^2+\tfrac74=0.
$$
Отсюда $\lambda_{1,2}=-\dfrac12\pm\dfrac{\sqrt7}{2}i$ — обе вещественные части отрицательны ($-1/2<0$). Нулевое решение **экспоненциально устойчиво**.

*Короткий путь:* в уравнении $\lambda^2+\lambda+2=0$ оба коэффициента ($1$ и $2$) положительны $\Rightarrow$ по критерию Рауса–Гурвица вещественные части корней отрицательны, можно даже не решать квадратное уравнение. $\blacksquare$

### Разбор заданий из образца КИМ (раздел III)

**Задача 2.** Исследовать на устойчивость нулевое решение системы
$$
\dot x=2(1-x)-2e^{y},\qquad \dot y = x+x^2y-3y.
$$
*Метод решения:* находим $A(0,0)$, дифференцируя $f_1,f_2$ по $x,y$ и подставляя $x=y=0$; далее — характеристическое уравнение и знаки вещественных частей корней. По ответу к образцу: $\lambda_{1,2}=-2\mp i$ — обе части отрицательны, нулевое решение **экспоненциально устойчиво**.

**Задача 3.** Система
$$
\dot x=2\cos(x+y)-2e^{y},\qquad \dot y=-x+x^2y+2\sin y.
$$
Ответ образца: $\lambda_{1,2}=1\mp i$ — вещественная часть положительна, нулевое решение **неустойчиво**.

*Общая схема решения обеих задач:* линеаризация в нуле (матрица частных производных при $x=y=0$), характеристическое уравнение $\lambda^2-\operatorname{tr}(A)\lambda+\det(A)=0$, и вывод по знаку вещественных частей корней (или сразу по знакам $\operatorname{tr}A$, $\det A$: устойчивость требует $\operatorname{tr}A<0$ и $\det A>0$).

---

## Итоговый чек-лист перед экзаменом

**Алгебра**
- [ ] Определения: линейное пространство, базис, размерность, линейный оператор, матрица оператора
- [ ] Формула смены матрицы оператора при смене базиса
- [ ] Метод Гаусса для СЛУ, ранг матрицы и теорема Кронекера–Капелли
- [ ] Вычисление определителя и собственных значений матрицы 3×3, обратная матрица

**Математический анализ**
- [ ] Критерий Коши сходимости последовательности; вычисление простых пределов
- [ ] Определения предела ФМП по Коши/Гейне, предел по кривой; способы доказать отсутствие предела
- [ ] Повторные пределы и их связь с двойным пределом (три контрпримера — примеры 4–6)
- [ ] Непрерывность по совокупности vs по отдельным переменным (примеры 8–9)
- [ ] Дифференцируемость: определение, необходимое и достаточное условия, связь с непрерывностью (теоремы 3–5)
- [ ] Смешанные производные и теорема о равенстве $f_{xy}''=f_{yx}''$
- [ ] Признаки сходимости числовых рядов: Даламбер, сравнение, Коши, Лейбниц; абсолютная vs условная сходимость
- [ ] Равномерная сходимость функциональных рядов, признак Вейерштрасса, теорема о непрерывности суммы

**Дифференциальные уравнения**
- [ ] Теорема Коши–Пикара: условия (непрерывность по $t$, условие Липшица по $x$) и вывод (существование и единственность)
- [ ] Уметь строить контрпример нарушения условия Липшица (пример 2)
- [ ] Алгоритм исследования устойчивости по первому приближению (матрица Якоби в нуле → собственные значения → знак вещественных частей)
- [ ] Критерий Рауса–Гурвица для 2×2 как быстрый способ не решать характеристическое уравнение явно

**Формат билета:** первые два вопроса каждого раздела — теоретические (определение/формулировка теоремы без доказательства, кроме раздела ДУ, где первый вопрос раздела III оценивается в 10 баллов и может требовать больше деталей); дальнейшие задания — тест с обязательным обоснованием выбора либо вычислительная задача. Обоснование варианта ответа так же важно, как и сам выбор — терять баллы на «угадал, но не объяснил» не стоит.
